# Система анализа пользовательских отзывов (Django + Pandas + ML + Yandex DataLens)

Проект: веб-сервис для загрузки отзывов, их предобработки, анализа тональности и построения интерактивных дашбордов.

## Быстрый старт (локальная разработка)
```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .\.venv\Scripts\Activate.ps1
pip install --upgrade pip
pip install -r requirements.txt
